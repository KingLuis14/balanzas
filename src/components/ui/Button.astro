<!-- ---
import type { HTMLTag, Polymorphic } from 'astro/types';
import { cva, type VariantProps } from 'class-variance-authority';
const button = cva('button', {
   variants: {
      theme: {
         normal: 'button--normal',
         theme2: 'button--theme2',
         theme3: 'button--theme3',
         disable: 'button--disable',
         product: 'button--product',
      },
      size: {
         content: 'button--content',
      },
   },
});

type Props<Tag extends HTMLTag> = Polymorphic<{ as: Tag; classes?: string }> &
   VariantProps<typeof button>;

const { as: Tag, theme = 'normal', size, classes, ...attrs } = Astro.props;
---

<Tag class={button({ theme, size, class: classes })} {...attrs} >
   <slot />
</Tag>

<style lang="scss" is:global>
   .button {
      background-color: var(--button-bg-color, #fff);
      padding: 0.5em 1.5em;
      border-radius: 0.25em;
      color: var(--button-text-color, #000);
      font-weight: 600;

      &--content {
         width: fit-content;
      }

      &:is(:hover, :focus) {
         background-color: var(--button-bg-color-hover, #000);
         color: var(--button-text-color-hover, #fff);
      }

      &--product{
         --button-text-color: black;
         --button-bg-color: rgb(210, 210, 210);
         --button-text-color-hover: black;
         --button-bg-color-hover: rgb(166, 166, 166);
         padding: 0.3em .8em;
      border-radius: 0;
      }

      &--normal {
         --button-text-color: var(--black-000);
         --button-bg-color: var(--green-500);
         --button-text-color-hover: var(--black-000);
         --button-bg-color-hover: var(--green-600);
      }

      &--theme2 {
         --button-text-color: var(--blue-500);
         --button-bg-color: var(--white-000);
         --button-bg-color-hover: var(--white-100);
         --button-text-color-hover: var(--black-100);
      }

      &--disable {
         --button-text-color: var(--blue-500);
         --button-bg-color: var(--blue-950);
         --button-text-color-hover: var(--blue-500);
         --button-bg-color-hover: var(--blue-950);
      }

      &--disable[data-theme='ligth'] {
         --button-text-color: var(--blue-500);
         --button-bg-color: var(--blue-300);
         --button-text-color-hover: var(--blue-500);
         --button-bg-color-hover: var(--blue-300);
      }
   }
</style> -->
